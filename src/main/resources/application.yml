spring:
  datasource:
    url: jdbc:postgresql://dpg-d1338h49c44c738s5qrg-a.oregon-postgres.render.com/postgresdb_bz71
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
